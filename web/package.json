{
  "name": "web",
  "version": "1.0.0",
  "scripts": {
    "serve": "esbuild --bundle --format=esm --sourcemap --outdir=. --servedir=. main.ts worker-main.ts",
    "xbuild": "esbuild --bundle --format=esm --sourcemap --outfile=../deploy/bundle.js main.ts"
  },
  "devDependencies": {
    "dprint": "^0.32.1",
    "esbuild": "^0.15.7",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "preact": "^10.11.0"
  }
}
