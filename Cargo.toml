[workspace]
members = [
  "cli",
  "exe/gdi",
  "web/glue",
  "win32/derive",
]

[workspace.dependencies]
log = { path = "log" }
memory = { path = "memory" }
win32 = { path = "win32" }
x86 = { path = "x86" }

[profile.release]
lto = true
